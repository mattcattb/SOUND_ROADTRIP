FROM oven/bun:1.3.13-alpine

WORKDIR /app

COPY . .

RUN bun install --frozen-lockfile \
  && bun run --filter @spotify-roadtrip/server build

ENV NODE_ENV=production

CMD ["bun", "run", "--filter", "@spotify-roadtrip/server", "start"]
